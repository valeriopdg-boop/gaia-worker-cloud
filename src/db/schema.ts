import {
  boolean,
  date,
  index,
  integer,
  pgTable,
  text,
  timestamp,
  uuid
} from "drizzle-orm/pg-core";

// TODO: incolla qui lo schema minimo (tenants, users, enterprises, workers, workerClouds, workerConnections, documents, workerArchiveDocuments)
